export namespace DEFAULT_RUN_PARAMS {
    const projectPoints: boolean;
    const viewport: null;
    const createBufferObjects: boolean;
    const moduleSettings: {};
}
export const MAX_32_BIT_FLOAT: 3.402823466e+38;
export const MIN_BLEND_EQUATION: number[];
export const MAX_BLEND_EQUATION: number[];
export const MAX_MIN_BLEND_EQUATION: number[];
export const EQUATION_MAP: {
    [x: number]: number | number[];
};
export const ELEMENTCOUNT: 4;
export namespace DEFAULT_WEIGHT_PARAMS {
    const size: number;
    const operation: number;
    const needMin: boolean;
    const needMax: boolean;
    const combineMaxMin: boolean;
}
export const PIXEL_SIZE: 4;
export const WEIGHT_SIZE: 3;
export namespace MAX_MIN_TEXTURE_OPTS {
    const format: number;
    const type: number;
    const border: number;
    const mipmaps: boolean;
    const parameters: {
        [x: number]: number;
    };
    const dataFormat: number;
    const width: number;
    const height: number;
}
//# sourceMappingURL=gpu-grid-aggregator-constants.d.ts.map