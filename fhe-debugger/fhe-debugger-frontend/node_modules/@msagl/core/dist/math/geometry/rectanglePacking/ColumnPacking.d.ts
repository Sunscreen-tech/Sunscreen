import { Rectangle } from '../rectangle';
import { Packing } from './Packing';
export declare class ColumnPacking extends Packing {
    orderedRectangles: Rectangle[];
    getRects(): Rectangle[];
    maxHeight: number;
    constructor(rectangles: Rectangle[], maxHeight: number);
    run(): void;
}
