import { Algorithm } from '../../../utils/algorithm';
import { Point } from '../point';
import { Rectangle } from '../rectangle';
export declare abstract class Packing extends Algorithm {
    rectsToCenters: Map<Rectangle, Point>;
    private packedWidth;
    get PackedWidth(): number;
    set PackedWidth(value: number);
    private packedHeight;
    get PackedHeight(): number;
    set PackedHeight(value: number);
    get PackedAspectRatio(): number;
    getRects(): Rectangle[];
}
