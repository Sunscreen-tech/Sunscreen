import { Rectangle } from '../rectangle';
import { Packing } from './Packing';
export declare class GreedyRectanglePacking extends Packing {
    rectanglesByDescendingHeight: Rectangle[];
    wrapWidth: number;
    constructor(rectangles: Rectangle[], wrapWidth: number, rectanglesPresorted?: boolean);
    static SortRectangles(rectangles: Rectangle[]): Rectangle[];
    run(): void;
    Pack(): void;
}
