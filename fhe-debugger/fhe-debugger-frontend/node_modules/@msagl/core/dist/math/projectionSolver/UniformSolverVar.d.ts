export declare class UniformSolverVar {
    lowBound: number;
    upperBound: number;
    IsFixed: boolean;
    position: number;
    Width: number;
    get Position(): number;
    set Position(value: number);
    get LowBound(): number;
    set LowBound(value: number);
    get UpperBound(): number;
    set UpperBound(value: number);
    toString(): string;
}
