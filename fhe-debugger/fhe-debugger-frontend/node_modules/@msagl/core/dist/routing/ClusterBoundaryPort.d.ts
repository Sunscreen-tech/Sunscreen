import { ICurve, Point, Polyline } from '../math/geometry';
import { RelativeFloatingPort } from '../layout/core/relativeFloatingPort';
export declare class ClusterBoundaryPort extends RelativeFloatingPort {
    loosePolyline: Polyline;
    get LoosePolyline(): Polyline;
    set LoosePolyline(value: Polyline);
    constructor(curveDelegate: () => ICurve, centerDelegate: () => Point, locationOffset?: Point);
    static mk(curveDelegate: () => ICurve, centerDelegate: () => Point): ClusterBoundaryPort;
}
