import { GeomNode } from '../layout/core';
import { ICurve } from '../math/geometry';
import { Shape } from './shape';
export declare class RelativeShape extends Shape {
    node: GeomNode;
    get BoundaryCurve(): ICurve;
    set BoundaryCurve(value: ICurve);
    curveDelegate: () => ICurve;
    constructor(node: GeomNode);
}
