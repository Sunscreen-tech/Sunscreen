import { Point } from '../../math/geometry/point';
import { SweepEvent } from '../spline/coneSpanner/SweepEvent';
export declare class AxisCoordinateEvent extends SweepEvent {
    private site;
    constructor(p: Point);
    get Site(): Point;
}
