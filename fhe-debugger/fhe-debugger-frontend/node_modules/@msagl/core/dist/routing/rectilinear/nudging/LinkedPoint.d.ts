import { Point } from '../../../math/geometry/point';
export declare class LinkedPoint {
    Point: Point;
    Next: LinkedPoint;
    constructor(point: Point);
    GetEnumerator(): IterableIterator<Point>;
    get X(): number;
    get Y(): number;
    InsertVerts(i: number, j: number, points: Point[]): void;
    InsertVertsInReverse(i: number, j: number, points: Point[]): void;
    SetNewNext(p: Point): void;
}
