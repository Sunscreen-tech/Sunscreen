import { Point } from '../../../math/geometry/point';
import { Comparer } from '@esfx/equatable';
export declare class PointByDelegateComparer implements Comparer<Point> {
    projection: (p: Point) => number;
    constructor(projection: (p: Point) => number);
    compare(x: Point, y: Point): number;
}
