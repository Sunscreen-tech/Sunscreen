import { Port } from '../layout/core/port';
import { ICurve } from '../math/geometry/icurve';
import { Rectangle } from '../math/geometry/rectangle';
export declare class Shape {
    private parents;
    private children;
    get Parents(): Array<Shape>;
    get Children(): Array<Shape>;
    get BoundaryCurve(): ICurve;
    set BoundaryCurve(value: ICurve);
    private boundaryCurve;
    get BoundingBox(): Rectangle;
    get Ports(): Set<Port>;
    private ports;
    UserData: any;
    static mkShape(): Shape;
    /**  Constructor taking the curve of the shape.*/
    constructor(boundaryCurve?: ICurve);
    /**  A group is a shape that has children.*/
    get IsGroup(): boolean;
    IsTransparent: boolean;
    Descendants(): IterableIterator<Shape>;
    Ancestors(): IterableIterator<Shape>;
    AddParent(shape: Shape): void;
    AddChild(shape: Shape): void;
    RemoveChild(shape: Shape): void;
    RemoveParent(shape: Shape): void;
    ToString(): string;
}
