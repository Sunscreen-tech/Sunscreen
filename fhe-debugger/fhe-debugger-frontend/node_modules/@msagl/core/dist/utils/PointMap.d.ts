import { Point } from '../math/geometry/point';
export declare class PointMap<T> {
    mapOfMaps: Map<number, Map<number, T>>;
    private size_;
    deleteP(point: Point): boolean;
    clear(): void;
    get size(): number;
    setxy(x: number, y: number, v: T): void;
    set(p: Point, v: T): void;
    delete(x: number, y: number): boolean;
    hasxy(x: number, y: number): boolean;
    has(p: Point): boolean;
    getxy(x: number, y: number): T;
    get(p: Point): T;
    constructor();
    keys(): IterableIterator<Point>;
    [Symbol.iterator](): IterableIterator<[Point, T]>;
    values(): IterableIterator<T>;
}
