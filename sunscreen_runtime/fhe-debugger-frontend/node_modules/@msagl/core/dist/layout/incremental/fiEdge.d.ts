import { Point } from '../../math/geometry';
import { FiNode } from './fiNode';
import { IEdge } from '../../structs/iedge';
import { GeomEdge } from '../core';
export declare class FiEdge implements IEdge {
    mEdge: GeomEdge;
    sourceFiNode: FiNode;
    targetFiNode: FiNode;
    constructor(mEdge: GeomEdge);
    get source(): number;
    get target(): number;
    private _length;
    get length(): number;
    set length(value: number);
    vector(): Point;
}
