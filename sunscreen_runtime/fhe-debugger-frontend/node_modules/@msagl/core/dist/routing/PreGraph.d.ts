import { GeomEdge, ICurve, Rectangle } from '..';
export declare class PreGraph {
    edges: Array<GeomEdge>;
    nodeBoundaries: Set<ICurve>;
    boundingBox: Rectangle;
    static constructorStatic(egs: GeomEdge[], nodeBoundaries: Set<ICurve>): PreGraph;
    AddGraph(a: PreGraph): void;
    AddNodeBoundary(curve: ICurve): void;
}
