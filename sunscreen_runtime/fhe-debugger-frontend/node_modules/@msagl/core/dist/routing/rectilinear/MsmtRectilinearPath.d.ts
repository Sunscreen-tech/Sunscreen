import { Point } from '../../math/geometry/point';
import { VisibilityVertex } from '../visibility/VisibilityVertex';
export declare class MsmtRectilinearPath {
    private bendPenaltyAsAPercentageOfDistance;
    private currentPassTargetEntries;
    constructor(bendPenalty: number);
    GetPath(sources: Array<VisibilityVertex>, targets: Array<VisibilityVertex>): Array<Point>;
    private GetPathStage;
    private static UpdateTargetEntriesForEachDirection;
    private static Barycenter;
}
