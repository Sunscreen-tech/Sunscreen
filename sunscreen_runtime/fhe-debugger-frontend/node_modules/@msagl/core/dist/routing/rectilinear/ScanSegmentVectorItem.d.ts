import { Point } from '../../math/geometry/point';
import { ScanSegment } from './ScanSegment';
export declare class ScanSegmentVectorItem {
    FirstSegment: ScanSegment;
    CurrentSegment: ScanSegment;
    private pendingPerpCoords;
    AddPendingPerpendicularCoord(coord: number): void;
    ResetForIntersections(): void;
    get IsHorizontal(): boolean;
    Coord: number;
    constructor(coord: number);
    TraverseToSegmentContainingPoint(point: Point): boolean;
    MoveNext(): boolean;
    get HasCurrent(): boolean;
    PointIsCurrentEndAndNextStart(point: Point): boolean;
    AddPerpendicularCoord(perpCoord: number): void;
    toString(): string;
    AppendScanSegment(segment: ScanSegment): void;
    AddPendingPerpendicularCoordsToScanSegments(): void;
}
