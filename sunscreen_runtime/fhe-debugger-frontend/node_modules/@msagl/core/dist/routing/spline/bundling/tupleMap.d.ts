export declare class TupleMap<A, B, C> {
    mainMap: Map<A, Map<B, C>>;
    get isEmpty(): boolean;
    everyMapIsEmpty(): boolean;
    get(a: A, b: B): C;
    has(a: A, b: B): boolean;
    set(a: A, b: B, c: C): void;
    [Symbol.iterator](): IterableIterator<[A, B, C]>;
    keys(): IterableIterator<[A, B]>;
}
