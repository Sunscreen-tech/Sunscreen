import { Point } from '../../math/geometry/point';
import { SweepEvent } from '../spline/coneSpanner/SweepEvent';
export declare class PortObstacleEvent extends SweepEvent {
    site: Point;
    constructor(site: Point);
    get Site(): Point;
}
