export declare class UnimodalSequence {
    f: (m: number) => number;
    get Sequence(): (m: number) => number;
    set Sequence(value: (m: number) => number);
    length: number;
    get Length(): number;
    set Length(value: number);
    constructor(sequenceDelegate: (m: number) => number, length: number);
    FindMinimum(): number;
    private BehaviourAtIndex;
    FindMaximum(): number;
}
