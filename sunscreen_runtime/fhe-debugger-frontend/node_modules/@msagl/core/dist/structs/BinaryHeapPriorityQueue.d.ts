export declare class BinaryHeapPriorityQueue {
    _heap: number[];
    _reverse_heap: number[];
    _priors: number[];
    get Count(): number;
    heapSize: number;
    constructor(n: number);
    SwapWithParent(i: number): void;
    Enqueue(o: number, priority: number): void;
    PutAtI(i: number, h: number): void;
    Dequeue(): number;
    IsEmpty(): boolean;
    DecreasePriority(o: number, newPriority: number): void;
}
