export declare class CoupleSet {
    mapOfSets: Map<number, Set<number>>;
    insert(x: number, y: number): void;
    delete(x: number, y: number): void;
    has(x: number, y: number): boolean;
    constructor();
    elems(): IterableIterator<[number, number]>;
}
