import { IntPair } from './IntPair';
export declare class IntPairSet {
    has(p: IntPair): boolean;
    arrayOfSets: Set<number>[];
    remove(p: IntPair): boolean;
    hasxy(x: number, y: number): boolean;
    constructor();
    static mk(ps: Array<IntPair>): IntPairSet;
    values(): IterableIterator<IntPair>;
    add(p: IntPair): void;
    addNN(x: number, y: number): void;
    clear(): void;
}
