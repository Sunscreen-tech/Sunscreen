import { Point } from '..';
import { PointPair } from '../math/geometry/pointPair';
import { PointMap } from './PointMap';
export declare class PointPairMap<T> {
    mapOfMaps: PointMap<PointMap<T>>;
    private size_;
    clear(): void;
    get size(): number;
    set(pp: PointPair, v: T): void;
    delete(pp: PointPair): void;
    has(pp: PointPair): boolean;
    getPP(p: Point, q: Point): T;
    get(pp: PointPair): T;
    constructor();
    keys(): IterableIterator<PointPair>;
    [Symbol.iterator](): IterableIterator<[PointPair, T]>;
    values(): IterableIterator<T>;
}
