import { Graph as JSONGraph } from 'dotparser';
import { Graph } from '@msagl/core';
import { ArrowTypeEnum, ShapeEnum } from '@msagl/drawing';
/** parses a string representing a Graph in DOT format */
export declare function parseDot(graphStr: string): Graph;
/** parses JSONGraph type to a Graph */
export declare function parseJSONGraph(jsonObj: JSONGraph): Graph;
/** Exports the graph into a JSONGraph object for further serialization */
export declare function graphToJSON(graph: Graph): JSONGraph;
/** Each line of the file is a string in format sourceId\ttargetId. That is two words separated by a tabulation symbol.
 * The edges are considered directed.
 */
export declare function parseTXT(content: string): Graph;
export declare function loadGraphFromFile(file: File): Promise<Graph>;
export declare function loadGraphFromUrl(url: string): Promise<Graph>;
declare type SimpleJSONGraph = {
    /** List of nodes in the graph */
    nodes: {
        /** Id of the node */
        id: number | string;
        /** Weight of the node */
        weight?: number;
        /** Label text of the node */
        label?: string;
        /** Shape of the node. Default `box` */
        shape?: keyof typeof ShapeEnum;
        /** [CSS color](https://developer.mozilla.org/en-US/docs/Web/CSS/color) of the node */
        color?: string;
    }[];
    /** List of edges in the graph */
    edges: {
        /** Id of the source node */
        source: number | string;
        /** Id of the target node */
        target: number | string;
        /** Whether the edge is directed. Default `true` */
        directed?: boolean;
        /** Weight of the edge */
        weight?: number;
        /** Type of the arrow at the source. Default `none` */
        arrowhead?: keyof typeof ArrowTypeEnum;
        /** Type of the arrow at the target. Default `none` */
        arrowtail?: keyof typeof ArrowTypeEnum;
        /** [CSS color](https://developer.mozilla.org/en-US/docs/Web/CSS/color) of the edge */
        color?: string;
    }[];
};
export declare function parseJSON(json: JSONGraph | SimpleJSONGraph): Graph;
export declare function parseSimpleJSON(json: SimpleJSONGraph): Graph;
export {};
