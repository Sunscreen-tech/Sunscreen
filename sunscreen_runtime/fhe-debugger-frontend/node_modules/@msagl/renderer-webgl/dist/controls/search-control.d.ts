import type Renderer from '../renderer';
import type { IRendererControl } from '../renderer';
export default class SearchControl implements IRendererControl {
    private _dropdown;
    private _nodes;
    onAdd(renderer: Renderer): void;
    onRemove(renderer: Renderer): void;
    getElement(): HTMLDivElement;
    private _onGraphLoad;
    private _updateNodeList;
}
